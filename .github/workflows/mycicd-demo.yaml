name: My first Workflow

on:
  push:
    branches: [main]
    
jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: Say Hello
          run: |
            echo "Hello there, welcome to my CICD demo"
            echo "I am putting my hands to try out workflows in CICD"
            echo "This training is from Hagital Consults"
            echo "I am learning to create workflows in CICD"
          
        
