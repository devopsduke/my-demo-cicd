name: My first Workflow

on:
  push:
    branches: [main]
    
jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@4
        - name: Say Hello
          run: echo " Say Hello, To the world of CI/CD"
          
        
